package ${package_name}.rpc.api;

import com.tudou.common.base.BaseServiceMock;
import ${package_name}.dao.mapper.${vmmodel}Mapper;
import ${package_name}.dao.model.${vmmodel};
import ${package_name}.dao.model.${vmmodel}Example;

/**
* 降级实现${vmmodel}Service接口
* Created by tudou on ${ctime}.
*/
public class ${vmmodel}ServiceMock extends BaseServiceMock<${vmmodel}Mapper, ${vmmodel}, ${vmmodel}Example> implements ${vmmodel}Service {

}
